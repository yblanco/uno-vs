upstream client {
  server        client:80;
}

server {
  listen        443 ssl;
  server_name   uno-vs.com;

  include       /etc/nginx/common.conf;
  include       /etc/nginx/ssl.conf;
  ssl_certificate             /srv/ssl/client/cert/fullchain.pem;
  ssl_certificate_key         /srv/ssl/client/cert/privkey.pem;

  location / {
    proxy_pass  http://client;
  }
}
